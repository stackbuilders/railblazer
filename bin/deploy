#!/bin/bash

# Deploy a build

if [ $# -ne 1 ]
then
	echo "USAGE: scriptname DEPLOY_ENVIRONMENT"
	exit 1
fi

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" 

cd ${WORKSPACE} && cap $1 deploy:migrations